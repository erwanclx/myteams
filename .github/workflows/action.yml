name: MyTeams Make CI | Erwan

on:
  push:
    branches: ["erwan"]
  pull_request:
    branches: ["erwan"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: sudo apt-get install -y libncurses5-dev libncursesw5-dev libcurl4-dev
      - name: make
        run: make
